---
name: visual-editing
description: "Controlled editing prompt skill for image replacement, cleanup, restyling, compositing and inpainting."
---
# Visual Editing
Use the edit equation:
Change = exact target + desired state.
Keep = identity + geometry + composition + lighting + relevant text.
Avoid = named failure modes.
For composites, assign each reference a single role.
For troubleshooting, change one logical variable at a time.
